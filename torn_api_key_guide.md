# Torn API Key 获取详细指南

**版本**: 1.0  
**日期**: 2026-02-07  
**目标**: 帮助获取 Torn API Key

---

## 📝 前置条件

### 必须完成
- ✅ 已注册 Torn 账号
- ✅ 已完成邮箱验证
- ✅ 已登录游戏

### 如果还没注册
1. 访问 https://www.torn.com
2. 点击右上角 **"Sign Up"**
3. 填写用户名、密码、邮箱
4. 去邮箱点击验证链接
5. 登录游戏完成新手教程（5-10分钟）

---

## 🔑 获取 API Key 步骤

### 方法1: 通过网页界面（推荐）

#### 步骤1: 访问 API 页面
```
网址: https://www.torn.com/api.html
```
或直接在游戏内导航到 API 页面

#### 步骤2: 登录账号
如果还没登录，先登录你的 Torn 账号

#### 步骤3: 生成 API Key
1. 在 API 页面找到 **"API Keys"** 或 **"Generate New Key"** 按钮
2. 点击 **"Create New Key"** 或 **"Generate Key"**

#### 步骤4: 配置权限
选择需要的 API 权限（建议全选）：

```
✅ Public         - 公开数据（必须）
✅ User           - 玩家个人信息
✅ Faction        - 派系数据
✅ Company        - 公司数据
✅ Market         - 市场数据
✅ Property       - 房产数据
✅ Stock Market   - 股票市场（重要）
✅ Item Market    - 物品市场
✅ Forums         - 论坛数据
✅ Torn           - 游戏基础数据
```

#### 步骤5: 命名和保存
1. 给 API Key 起个名字（例如: "OpenClawAI"）
2. 点击 **"Save"** 或 **"Generate"**
3. **立即复制 API Key**（只显示一次！）

#### 步骤6: 保存 API Key
```
格式: 16位字符串
示例: a1b2c3d4e5f6g7h8
```

**⚠️ 重要**: 
- API Key 只显示一次，务必保存！
- 如果丢失，需要重新生成
- 不要分享给不信任的人

---

### 方法2: 通过游戏内链接

#### 步骤1: 登录 Torn
访问 https://www.torn.com 并登录

#### 步骤2: 进入设置
1. 点击右上角你的用户名
2. 选择 **"Settings"** 或 **"Account"**
3. 找到 **"API"** 或 **"API Keys"** 选项

#### 步骤3: 生成 Key
按照方法1的步骤3-6操作

---

## 🔧 API Key 使用示例

### Python 代码测试
```python
import requests

# 你的 API Key
API_KEY = "你的16位API密钥"

# 基础请求URL
base_url = "https://api.torn.com"

# 1. 获取玩家基本信息
endpoint = f"{base_url}/user/"
params = {
    "key": API_KEY,
    "selections": "basic,profile"
}

response = requests.get(endpoint, params=params)
data = response.json()

if "error" in data:
    print(f"错误: {data['error']['error']}")
else:
    print(f"玩家名称: {data['name']}")
    print(f"玩家ID: {data['player_id']}")
    print(f"等级: {data['level']}")
    print(f"现金: ${data['money_onhand']:,}")

# 2. 获取股票信息
endpoint = f"{base_url}/user/"
params = {
    "key": API_KEY,
    "selections": "stocks"
}
response = requests.get(endpoint, params=params)
stock_data = response.json()
print(f"股票数据: {stock_data}")

# 3. 获取市场信息
endpoint = f"{base_url}/market/"
params = {
    "key": API_KEY,
    "id": "item_id_here"  # 物品ID
}
```

---

## 📊 常用 API 端点

### 玩家相关
```
/user/           - 玩家信息
/user/?selections=basic,profile,stocks,properties,company
```

### 股票相关
```
/torn/?selections=stocks         - 所有股票信息
/user/?selections=stocks         - 玩家持有的股票
```

### 公司相关
```
/company/        - 公司信息
/user/?selections=company        - 玩家的公司
```

### 市场相关
```
/market/         - 市场价格
/torn/?selections=items          - 物品列表
```

### 派系相关
```
/faction/        - 派系信息
/user/?selections=faction        - 玩家所在派系
```

---

## ⚠️ 重要限制

### 频率限制
```
- 每秒最多 100 次请求
- 超过限制会被临时封禁
- 建议添加延迟: time.sleep(0.1)
```

### 权限限制
```
- 某些操作需要特定权限
- 部分数据只对付费会员开放
- 确保选择了正确的权限
```

### 安全建议
```
✅ 不要将 API Key 上传到公开仓库
✅ 使用环境变量存储
✅ 定期更换 API Key
✅ 如果怀疑泄露，立即重新生成
```

---

## 🛠️ 常见问题

### Q1: API Key 显示 "Invalid Key"
**解决**:
- 检查是否复制完整
- 确认没有空格
- 重新生成新的 Key

### Q2: 提示 "Access Level Denied"
**解决**:
- 检查 API Key 的权限设置
- 确保选择了需要的权限
- 某些数据需要付费会员

### Q3: 请求频率过高被限制
**解决**:
```python
import time

# 添加延迟
for i in range(100):
    response = requests.get(url, params=params)
    time.sleep(0.1)  # 每次请求间隔0.1秒
```

### Q4: 无法找到 API 页面
**解决**:
- 确保已登录账号
- 直接访问: https://www.torn.com/api.html
- 或在游戏内搜索 "API"

---

## 🚀 获取 API Key 后

把 API Key 发给我，我立即帮你：

1. ✅ 测试 API 连接
2. ✅ 分析你的账户数据
3. ✅ 建立监控系统
4. ✅ 开发股票交易 AI
5. ✅ 创建犯罪优化器

---

## 📞 支持

如果遇到问题：
1. Torn 论坛: https://www.torn.com/forums.php
2. Torn Wiki: https://www.torn.com/wiki.php
3. 官方 Discord

---

**请按照步骤获取 API Key 后发给我！** 🦞🔑
